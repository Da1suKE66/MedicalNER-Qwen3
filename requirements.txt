python-dotenv
# ModelArts currently exposes a CUDA 12.x driver. Pin torch to a CUDA 12
# compatible wheel so pip does not pull newer CUDA 13 builds.
torch==2.6.0
transformers>=4.52.0,<5.0.0
datasets>=2.16.0,<4.0.0
accelerate>=0.26.0,<2.0.0
peft>=0.7.0,<1.0.0
bitsandbytes>=0.41.0,<0.50.0
scipy
tiktoken
einops
protobuf
llamafactory[metrics]==0.9.3
